{
  "id": "6dc6e464",
  "title": "Update TODO.md and VSCode Extension Syntax",
  "status": "completed",
  "created_at": "2026-02-09T08:39:57.121Z",
  "assigned_to_session": "b717ff1e-0d3e-46aa-8c1b-457d662e664a",
  "steps": []
}

# Plan: Update TODO.md and VSCode Extension Syntax

## Tasks

### 1. Update TODO.md
- Remove completed items:
  - `plugable ask app` → implemented as `askpass` feature
  - `override na SIMPLE_SAFE commands a WRAPPER_COMMANDS` → config rules have highest priority in analyzer.py (checked before allowlists)
  - `set final-rules ~/.dippy-final` → implemented as `set final` directive

### 2. Answer: Override SIMPLE_SAFE/WRAPPER_COMMANDS via `set`
- **Analysis**: SIMPLE_SAFE and WRAPPER_COMMANDS are hard-coded frozensets in `src/dippy/core/allowlists.py`
- **Current behavior**: Config rules (`allow`, `ask`, `deny`) are checked BEFORE SIMPLE_SAFE in `analyzer.py:656`
- **Answer**: No, cannot be disabled via `set` directive - these are hard-coded lists
- **Workaround**: Use config rules with explicit patterns - they take precedence

### 3. Update VSCode Extension Syntax
File: `editors/vscode/syntaxes/dippy.tmLanguage.json`

**Missing directives to add:**
- File operation rules: `allow-edit`, `ask-edit`, `deny-edit`, `allow-read`, `ask-read`, `deny-read`
- Include directive: `include`
- Wrapper directive: `wrapper`
- Option rules: `allow-opt`, `ask-opt`, `deny-opt`
- WebSearch rules: `allow-web`, `ask-web`, `deny-web`, `after-web`

**Current state:**
```json
"begin": "^\\s*(allow-redirect|ask-redirect|deny-redirect|allow-mcp|ask-mcp|deny-mcp|after-mcp|allow|ask|deny|after|alias)\\b"
```

**Needs update to:**
```json
"begin": "^\\s*(allow|ask|deny|after|include|wrapper|allow-redirect|ask-redirect|deny-redirect|allow-edit|ask-edit|deny-edit|allow-read|ask-read|deny-read|allow-opt|ask-opt|deny-opt|allow-mcp|ask-mcp|deny-mcp|after-mcp|allow-web|ask-web|deny-web|after-web)\\b"
```

### 4. Update version number
File: `editors/vscode/package.json`
- Update version from `0.0.7` to `0.0.8` (or increment appropriately)
