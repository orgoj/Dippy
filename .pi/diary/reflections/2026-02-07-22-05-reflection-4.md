# Reflection: Last 4 Entries

**Generated**: 2026-02-07 22:05
**Entries Analyzed**: 4
**Date Range**: 2026-02-04 to 2026-02-07

## Summary
The recent sessions focused on improving Dippy's integration with Claude Code and Gemini CLI, specifically addressing "annoying" permission prompts by synchronizing tool names and hook matchers. Key technical realizations include the universality of the `edit` directive for all modification tools and the necessity of explicit tool matching in the client settings (`settings.json`). There is a strong emphasis on process reliability: explaining changes before execution, using surgical edits over full writes for config files, and mandatory TDD.

## Patterns Identified

### Strong Patterns (3+ occurrences)
1. **Explain BEFORE acting** (4/4 entries)
   - Observation: User expects a clear explanation of *why* and *how* before any file modifications are made.
   - CLAUDE.md rule: `- communication: ALWAYS explain the intended change and the reasoning behind it FIRST. Wait for user approval before modifying any files.`

2. **Run tests before commit** (4/4 entries)
   - Observation: `just test` must be run and pass before any commit.
   - CLAUDE.md rule: `- commits: run `just test` BEFORE committing - NON-NEGOTIABLE.` (Already present, reinforced).

3. **Conventional Commits** (4/4 entries)
   - Observation: Consistent use of `feat:`, `fix:`, etc.
   - CLAUDE.md rule: `- commits: use conventional format (feat:, fix:, chore:, docs:)` (Already present).

4. **Czech Urgency Signals** (3/4 entries)
   - Observation: Phrases like "kurva" or "musi byt" indicate critical errors or requirements.
   - CLAUDE.md rule: `- communication: Czech phrases signal STOP - "kurva" (fundamental error), "musi byt" (non-negotiable).` (Already present).

### Emerging Patterns (2 occurrences)
1. **Prefer `edit` over `write`** (2/4 entries)
   - Observation: Destructive writes in configuration files led to accidental restoration of deleted items (e.g., `pal` MCP server).
   - CLAUDE.md rule: `- development: prefer surgical edit over full write for configuration files to prevent accidental regressions.`

2. **Dippy Directive Mapping** (2/4 entries)
   - Observation: Confusion between `Write` tool and `edit` rule. `allow-edit` is the universal directive for all modification tools.
   - CLAUDE.md rule: `- dippy: `allow-edit` is the universal directive for all modification tools (Write, Edit, MultiEdit).`

## Proposed CLAUDE.md Updates

### Communication
- ALWAYS explain the intended change and the reasoning behind it FIRST. Wait for user approval before modifying any files.

### Development
- Prefer surgical `edit` over full `write` for configuration files to prevent accidental regressions (e.g., reverting user's manual changes).

### Dippy Configuration
- Use `allow-edit` / `deny-edit` / `ask-edit` for all file modification tools (Write, Edit, MultiEdit).

## One-Off Observations
- Claude Code hooks only fire if the tool name exactly matches the `matcher` string in `settings.json`.
- `FILE_TOOL_NAMES` in `dippy.py` must be synchronized with what is in the hook `matcher`.
- standalone debug scripts should be placed in `scripts/debug/`.

## Metadata
- Entries analyzed:
    - 2026-02-07-13-17-DIPPY-CONFIG-REFINEMENT.md
    - 2026-02-07-13-06-DIPPY-SEARCH-TOOLS-SUPPORT.md
    - 2026-02-07-12-51-DIPPY-READ-FIX.md
    - 2026-02-04-14-15-GEMINI-INTEGRATION.md
