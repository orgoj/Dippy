# Session Diary

**Date**: 2026-01-21 14:30
**Session ID**: 1c67da11-7a2e-450f-9c01-021abd3383d3
**Project**: /home/michael/work/ai/CLAUDE/TOOLS/dippy

## Task Summary
User wanted to document the differences between the fork (orgoj/Dippy) and the upstream (ldayton/Dippy) by adding a concise "Fork Enhancements" section at the beginning of the README.

## Work Done
- Added "Fork Enhancements" section to README.md with 12 feature bullets
- Properly attributed features from different remotes (ldayton, tony)
- Committed and pushed changes to origin/orgoj

## Design Decisions
- **Section placement**: At the very beginning after title, before the main description - gives immediate visibility to fork-specific features
- **HTML comment markers**: Used `<!-- FORK ENHANCEMENTS START/END -->` for easy identification and future updates
- **Attribution format**: Used `*(by author)*` suffix for features from other contributors (tony)
- **Removed MCP rules**: Initially included but removed because it's in upstream (original) - we only cherry-picked it, not a fork enhancement

## Challenges & Solutions
| Challenge | Solution |
|-----------|----------|
| Identifying which features are from upstream vs fork | Used `git log original/main..HEAD` to filter fork-only commits |
| Confusion about MCP rules origin | Checked `git log original/main` - confirmed MCP is upstream feature |
| WebSearch attribution unclear | Checked `git log --all --source` to find `tony/tony/add-web-search-approve` branch |

## Mistakes & Corrections

### Where I Made Errors:
- Initially included MCP tool rules as fork enhancement when it's actually from upstream (ldayton)
- First attributed WebSearch as fork feature without checking tony remote
- Tried to use generic attribution `*(from upstream)*` instead of specific author name
- Only checked `original` remote initially, forgot about `tony` remote

### What Caused the Mistakes:
- Incomplete remote analysis - should have run `git remote -v` first
- Assumed all features in our branch that aren't in original are our own
- Didn't thoroughly check all commit sources with `--source` flag

## Lessons Learned

### Technical Lessons:
- Use `git log --all --oneline --source` to see which branch/remote each commit came from
- Multiple remotes require checking each one separately for attribution
- Cherry-picked commits from upstream shouldn't be listed as fork enhancements

### Process Lessons:
- When analyzing fork differences, first list ALL remotes with `git remote -v`
- Check commit sources before attributing features
- User's frustration signals ("kurva", "doprdele") = fundamental error requiring immediate correction

### To Remember for CLAUDE.md:
- Three remotes exist: origin (orgoj), original (ldayton), tony (tony-nekola-silk)
- Features from other contributors need proper attribution in documentation

## Skills & Commands Used

### Used in this session:
- [x] Command: `mop-diary:diary` - creating this diary entry

### Feedback for Skills/Commands:
| File | Issue/Observation | Suggested Fix/Action |
|------|-------------------|----------------------|
| N/A | Session was straightforward | N/A |

## User Preferences Observed

### Git & PR Preferences:
- Commit message: conventional format with `docs:` prefix for documentation changes
- Include Co-Authored-By trailer for Claude

### Code Quality Preferences:
- Accuracy in attribution is critical - user corrected multiple times until exact author names were used

### Technical Preferences:
- Czech communication when frustrated signals need for immediate correction
- Precise author attribution required (not generic "from upstream" but actual usernames)

## Code Patterns Used
- Git multi-remote analysis: `git remote -v`, `git log --all --source`
- Filtered commit history: `git log original/main..HEAD --oneline`

## Notes
- Final commit: `334415d docs: add fork enhancements section to README`
- 12 fork enhancements documented with proper attribution to tony for 2 features
- MCP rules correctly excluded (it's upstream feature we merged, not fork enhancement)
